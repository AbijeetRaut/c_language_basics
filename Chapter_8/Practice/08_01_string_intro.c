#include <stdio.h>
int main()
{
    char string[] = {'A', 'b', 'i', 'j', 'e', 'e', 't', '\0'};
    char string[] = "Abijeet"; 
     /* this are same and worked as the same.
        but in the second one /0 is automatically fitted,
        being the doubble cotted comma in array /0 is not used.
     */
     
    return 0;
}