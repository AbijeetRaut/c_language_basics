#include <stdio.h>

int main()
{

    int x = 3;
    int y = 4;

    printf("the value of 3*x - 3*y %d\n", 3 * x - 3 * y);
    /*
    as we see on the basis of note 3*x - 3*y works on the basis of the (3x) - (8y)
*/

    return 0;
}