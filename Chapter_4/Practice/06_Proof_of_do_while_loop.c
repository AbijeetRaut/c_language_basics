#include<stdio.h>
 
int main(){

    int a = 50;

    do{
        printf("proof of do_while loop because it runs and check the code %d\n", a);
    }while(a < 10); 

    //hence::::-
            // we have given the value of a is 50 but in while statement we has said that a is less 10.

return 0;
}